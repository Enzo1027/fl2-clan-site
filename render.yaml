services:
  - type: web
    name: fl2-clan-site
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: DATA_DIR
        value: /var/data
      - key: MASS_COMMS_PASSWORD
        sync: false
      - key: MASS_COMMS_CONTENT_KEY
        sync: false
      - key: MASS_COMMS_SESSION_SECRET
        sync: false
    disk:
      name: fl2-counter-data
      mountPath: /var/data
      sizeGB: 1
